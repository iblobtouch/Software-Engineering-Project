/**
 * The src package contains the main source files that constitutes the 
 * application body and links the interface with the operations. These files are
 * in charge of processing user input to execute the corresponding command 
 * class executors.
 */
package src;
