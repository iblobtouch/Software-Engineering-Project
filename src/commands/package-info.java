/**
 * The commands package contains the list of command executor classes for the 
 * fotoshop application. It uses the Command Pattern for a better segregation of 
 * commands and a cleaner approach to the implementation design.
 */
package commands;
